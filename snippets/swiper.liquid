{% comment %}
  Renders a swiping banner presets
  Accepts:
  - preset: {String} Swiper preset to use.
  - delay: {Number} Delay between slides in seconds.

  Usage:
  {% render 'swiper',
    swiper_preset: swiper_preset,
    delay: delay,
  %}
{% endcomment %}

{% style %}
    .slider-icon {
    padding-left: 1em;
    }
    .slider-icon svg {
    height: 28px;
    width: 24px;
    font-size: 10px;
    color: rgb(var(--color-foreground));
    }
  /*  .swiper-slide:not(.active) {
    display: none;
    }
    .swiper-slide-body {
    display: flex;
    direction: rtl;
    align-items: center;
    background-color: rgba(var(--color-foreground), 0.1);
    color: rgb(var(--color-foreground));
    padding: 0 10px;
    min-height: 100px;
    } */
{% endstyle %}

<div class="brand-badges fix-direction">
    {% case preset %}
        {% when "shipping_returns" %}
            <div class="swiper-container">
                <div class="swiper-slide">
                    <div class="swiper-slide-body">
                        <p class="product-badge__text">{{ 'products.product.brand_strength.delivery' | t }}</p>
                        <div class="slider-icon">
                            {% render 'icon-truck' %}
                        </div>
                    </div>
                </div>
                <div class="swiper-slide">
                    <div class="swiper-slide-body">
                        <p class="product-badge__text">{{ 'products.product.brand_strength.exchanges' | t }}</p>
                        <div class="slider-icon">
                            {% render 'icon-delivery-guy' %}
                        </div>
                    </div>
                </div>
                <div class="swiper-slide">
                    <div class="swiper-slide-body">
                        <p class="product-badge__text">{{ 'products.product.brand_strength.returns' | t }}</p>
                        <div class="slider-icon">
                            {% render 'icon-return' %}
                        </div>
                    </div>
                </div>
            </div>
    {% endcase %}
</div>